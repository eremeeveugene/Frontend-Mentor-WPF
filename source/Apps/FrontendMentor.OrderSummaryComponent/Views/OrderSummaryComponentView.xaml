<UserControl x:Class="FrontendMentor.OrderSummaryComponent.Views.OrderSummaryComponentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:FrontendMentor.OrderSummaryComponent.ViewModels"
             d:DataContext="{d:DesignInstance viewModels:OrderSummaryComponentViewModel}"
             x:ClassModifier="internal"
             mc:Ignorable="d">

    <Grid>
        <Grid Width="450" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Height="220"
                    Background="#382AE1"
                    CornerRadius="20 20 0 0" />

            <Border Grid.Row="1"
                    Padding="48,45,48,48"
                    Background="#FFFFFF"
                    CornerRadius="0 0 20 20">
                <StackPanel>
                    <TextBlock Text="Order Summary" />

                    <TextBlock Margin="0,17,0,0" Text="You can now listen to millions of songs, audiobooks, and podcasts on any device anywhere you like!" />

                    <Border Margin="0,20,0,0"
                            Background="#F7F9FF"
                            CornerRadius="11">
                        <Grid />
                    </Border>

                    <Button Height="50"
                            Margin="0,32,0,0"
                            Content="Proceed to Payment" />

                    <Button Margin="0,32,0,0" Content="Cancel Order" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>