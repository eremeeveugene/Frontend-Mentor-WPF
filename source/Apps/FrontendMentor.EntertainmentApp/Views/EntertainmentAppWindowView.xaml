<Window x:Class="FrontendMentor.EntertainmentApp.Views.EntertainmentAppWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:FrontendMentor.EntertainmentApp.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:EntertainmentAppWindowViewModel}"
        x:ClassModifier="internal">
    <Window.Resources>
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Header"
                    Value="{Binding Header}" />
        </Style>
    </Window.Resources>

    <TabControl TabStripPlacement="Left"
                ItemsSource="{Binding Tabs}" />
</Window>