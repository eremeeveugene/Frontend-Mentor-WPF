<UserControl x:Class="FrontendMentor.GitHubUserSearchApp.Views.GitHubUserSearchAppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:ClassModifier="internal"
             mc:Ignorable="d">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel>

            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="devfinder" />

                <TextBlock DockPanel.Dock="Right" Text="Dark" />
            </DockPanel>

            <TextBox />

            <Border Padding="48"
                    Background="#FEFEFE"
                    CornerRadius="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="1">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Left" Text="The Octocat" />

                            <TextBlock DockPanel.Dock="Right" Text="Joined 25 Jan 2011" />
                        </DockPanel>

                        <TextBlock Text="@octocat" />

                        <TextBlock Text="This profile has no bio" />

                        <Border Background="#F6F8FF " CornerRadius="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                            </Grid>
                        </Border>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Text="San Francisco" />

                            <TextBlock Grid.Row="0"
                                       Grid.Column="1"
                                       Text="https://github.blog" />

                            <TextBlock Grid.Row="1"
                                       Grid.Column="0"
                                       Text="Not Available" />

                            <TextBlock Grid.Row="1"
                                       Grid.Column="1"
                                       Text="@github" />
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>

</UserControl>
