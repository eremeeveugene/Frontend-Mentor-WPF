<views:SectorView x:Class="FrontendMentor.BlogPreviewCard.Views.BlogPreviewCardSectorView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:mvvm="http://prismlibrary.com/"
                  xmlns:viewModels="clr-namespace:FrontendMentor.BlogPreviewCard.ViewModels"
                  xmlns:views="clr-namespace:FrontendMentor.Assets.Controls.Views;assembly=FrontendMentor.Assets"
                  d:DataContext="{d:DesignInstance viewModels:BlogPreviewCardSectorViewModel}"
                  mvvm:ViewModelLocator.AutoWireViewModel="True"
                  x:ClassModifier="internal"
                  mc:Ignorable="d">
    <views:SectorView.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="CardBorder.Border.Brush" Color="{StaticResource Black.Color}" />
            <SolidColorBrush x:Key="CardBorder.Background.Brush" Color="{StaticResource PureWhite.Color}" />

        </ResourceDictionary>
    </views:SectorView.Resources>

    <Border Width="384"
            Padding="24"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{StaticResource CardBorder.Background.Brush}"
            BorderBrush="{StaticResource CardBorder.Border.Brush}"
            BorderThickness="1"
            CornerRadius="20">
        <StackPanel>
            <Rectangle Height="200"
                       RadiusX="10"
                       RadiusY="10" />

            <StackPanel>
                <Border CornerRadius="4">
                    <Border.Background>
                        <SolidColorBrush Color="{StaticResource Yellow.Color}" />
                    </Border.Background>

                    <TextBlock Text="Learning" />
                </Border>

                <TextBlock Text="Published 21 Dec 2023" />

                <TextBlock Text="HTML &amp; CSS foundations" />

                <TextBlock Text="These languages are the backbone of every website, defining structure, content, and presentation." />

                <StackPanel Orientation="Horizontal">
                    <Ellipse />


                    <TextBlock Text="Greg Hooper" />
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Border>
</views:SectorView>
